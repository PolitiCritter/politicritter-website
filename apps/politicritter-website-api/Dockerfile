# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build politicritter-website-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t politicritter-website-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system politicritter-website-api && \
          adduser --system -G politicritter-website-api politicritter-website-api

COPY dist/apps/politicritter-website-api politicritter-website-api/
RUN chown -R politicritter-website-api:politicritter-website-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix politicritter-website-api --omit=dev -f install

CMD [ "node", "politicritter-website-api" ]
